
# BACK-API-CROSSLEX
DATABASE_URL="postgresql://crosslex:crosslex@localhost:5432/crosslex?schema=public"
# Separate Postgres schema on the same container, used only by e2e tests.
TEST_DATABASE_URL="postgresql://crosslex:crosslex@localhost:5432/crosslex?schema=test"
JWT_KEY_SOURCE=file
JWT_PRIVATE_KEY_PATH=
JWT_PUBLIC_KEY_PATH=
# JWT_PRIVATE_KEY_SECRET_ID=
# JWT_PUBLIC_KEY_SECRET_ID=
# Both optional — fall back to 15m / 30 days if unset.
JWT_ACCESS_TOKEN_TTL=
REFRESH_TOKEN_TTL_DAYS=
COOKIE_DOMAIN=crosslex.local
